# ESP32-S3-KORVO-1 BSP 组件报告

> 基于路径：/Users/hph/Codes/esp-bsp/bsp/esp32_s3_korvo_1 的内容整理

## 一、项目概览
- 组件名称：ESP-BSP（Board Support Package）- ESP32-S3-KORVO-1
- 定位：为 ESP32-S3-KORVO-1 开发板提供统一、开箱即用的板级支持（I2C、音频编解码、按键、LED、SD 卡、SPIFFS 等）
- 主要特性：
  - 音频输入（ES7210）/输出（ES8311）编解码集成，I2S0（播放）、I2S1（采集，TDM）
  - 6 路模拟按键（经 ADC1_CH7/GPIO8）
  - 12 颗 WS2812 RGB 灯（GPIO19）
  - SD 卡（SDMMC 1-bit 或 SPI 模式）与 SPIFFS 文件系统挂载
  - I2C 总线（新驱动 I2C Driver-NG）
- 支持状态（Capabilities）：DISPLAY: 否；TOUCH: 否；BUTTONS: 是；AUDIO: 是；AUDIO_SPEAKER: 是；AUDIO_MIC: 是；SDCARD: 是；IMU: 否；LED: 是

## 二、目录结构与关键文件
- README.md：板卡概述、能力矩阵与可用示例链接
- API.md：按功能分组的 API 说明（I2C、ADC、音频、按钮、LED、SD/SPIFSS）
- Kconfig：可配置项（I2C 端口/速率、SD/SPIFFS 挂载点、是否失败格式化等）
- CMakeLists.txt：按 IDF 版本选择所需驱动；注册组件、包含与依赖
- idf_component.yml：组件元数据（version、targets、依赖、示例）
- include/bsp/esp32_s3_korvo_1.h：对外可见的宏定义、API 原型（引脚映射、功能开关、函数接口）
- esp32_s3_korvo_1.c / esp32_s3_korvo_1_idf5.c：主要功能实现（I2C、音频、按钮、LED、SD、SPIFFS、ADC 等）
- led_blink_defaults.c：LED 预设效果（常亮、快闪、慢闪、快呼吸、慢呼吸）
- priv_include/bsp_err_check.h：错误检查与断言封装（可由 Kconfig 控制是否 assert）
- doc/：板卡图片（用于 README 展示）

## 三、硬件资源与引脚映射（摘自头文件）
- I2C：SCL=GPIO2，SDA=GPIO1；`BSP_I2C_NUM` 由 Kconfig 选择（默认 0）
- 音频 I2S：
  - I2S0（播放）：BCLK=GPIO40，MCLK=GPIO42，LCLK=GPIO41，DOUT=GPIO39，DIN=NC
  - I2S1（采集/TDM）：BCLK=GPIO10，MCLK=GPIO20，LCLK=GPIO9，DOUT=NC，DIN=GPIO11
  - 功放使能：`BSP_POWER_AMP_IO=GPIO38`
- LED：WS2812 串灯，数据脚 `BSP_LED_RGB_GPIO=GPIO19`，数量 `BSP_LED_NUM=12`
- 按键：6 个多按键电阻分压接入 `BSP_BUTTONS_IO=GPIO8`（ADC1_CH7），板内阈值已在源码配置
- SD 卡：
  - SDMMC（1-bit）：D0=GPIO16，D3=GPIO15，CMD=GPIO17，CLK=GPIO18
  - SPI：MISO=GPIO16，CS=GPIO15，MOSI=GPIO17，CLK=GPIO18；`BSP_SDSPI_HOST=SDSPI_DEFAULT_HOST`
- SPIFFS：挂载点由 `CONFIG_BSP_SPIFFS_MOUNT_POINT` 指定（默认 /spiffs）

## 四、功能模块与 API 摘要
- I2C
  - 接口：`bsp_i2c_init() / bsp_i2c_deinit() / bsp_i2c_get_handle()`
  - 说明：使用 IDF Driver‑NG（`i2c_new_master_bus`）；句柄可复用在编解码器等 I2C 设备上
- ADC（IDF5+）
  - 接口：`bsp_adc_initialize() / bsp_adc_get_handle()`；`BSP_ADC_UNIT=ADC_UNIT_1`
  - 用途：按键电压读取（button_adc）
- 音频（esp_codec_dev 统一封装）
  - 初始化：`bsp_audio_init(const i2s_std_config_t *cfg)`（默认：播放 22.05kHz 单声道；采集 16kHz TDM）
  - 获取 I2S 数据接口：`bsp_audio_get_codec_itf_spk()` / `bsp_audio_get_codec_itf_mic()`
  - 编解码器设备：`bsp_audio_codec_speaker_init()`（ES8311）/ `bsp_audio_codec_microphone_init()`（ES7210）
  - 使用方式：初始化后调用 `esp_codec_dev_*` 族 API 进行播放/录音、音量/增益设置
- 按钮（espressif/button）
  - 接口：`bsp_iot_button_create(button_handle_t[], int *cnt, int size)`
  - 枚举：`bsp_button_t` 定义 6 个键位（REC/MODE/PLAY/SET/VOLDOWN/VOLUP）
  - 实现：通过 `button_adc` 在 ADC1_CH7 上区分不同按键阈值
- LED（espressif/led_indicator + strips）
  - 接口：`bsp_led_indicator_create(led_indicator_handle_t[], ...)`
  - 预设效果：ON/OFF/BLINK_FAST/BLINK_SLOW/BREATHE_FAST/BREATHE_SLOW
  - 实现：RMT 驱动 WS2812（可 DMA），配置在 `led_indicator_strips.h`
- 存储
  - SPIFFS：`bsp_spiffs_mount()` / `bsp_spiffs_unmount()`；挂载点 `BSP_SPIFFS_MOUNT_POINT`
  - SD 卡：
    - 默认：`bsp_sdcard_mount()`（SDMMC 1-bit）/ `bsp_sdcard_unmount()`
    - 自定义：`bsp_sdcard_sdmmc_mount(cfg)` / `bsp_sdcard_sdspi_mount(cfg)`，并提供 `get_*_host/slot` 助手
    - 句柄：`bsp_sdcard_get_handle()` 可配合 `sdmmc_card_print_info()` 输出卡信息

## 五、配置项（Kconfig）要点
- I2C
  - `BSP_I2C_NUM`（0/1，默认 0）
  - `BSP_I2C_FAST_MODE`（400kHz，默认开）/ `BSP_I2C_CLK_SPEED_HZ`
- SD 卡（VFS）
  - `BSP_SD_FORMAT_ON_MOUNT_FAIL`（挂载失败是否格式化）
  - `BSP_SD_MOUNT_POINT`（默认 `/sdcard`）/ `BSP_SD_MAX_FILES`
- SPIFFS（VFS）
  - `BSP_SPIFFS_FORMAT_ON_MOUNT_FAIL` / `BSP_SPIFFS_MOUNT_POINT`（默认 `/spiffs`）
  - `BSP_SPIFFS_PARTITION_LABEL`（默认 `storage`）/ `BSP_SPIFFS_MAX_FILES`
- 错误检查
  - `BSP_ERROR_CHECK`：启用后非 0 直接 `ESP_ERROR_CHECK` 断言；否则返回错误码

## 六、依赖与兼容性
- 目标芯片：`esp32s3`
- ESP‑IDF：`>= 5.3`（来自 idf_component.yml；原因：使用 I2C Driver‑NG 且 esp_codec_dev 最低支持 5.3）
- 组件依赖：
  - `espressif/led_indicator ~2.0`
  - `espressif/button ^4`
  - `espressif/esp_codec_dev ~1.5`
  - IDF 驱动：`esp_driver_i2c/gpio/i2s/sdmmc/sdspi` 或旧版 `driver`（CMake 按 IDF 版本选择）
- 备注：源码保留了对 <5.3 的一定兼容处理（CMake/条件编译），但通过组件管理器使用时建议遵循 `>=5.3`

## 七、示例与快速上手
- 官方示例：`examples/audio`（播放与录音 WAV），README 提供了 ESP Launchpad 一键烧录链接
- 快速使用片段：
  - 音频播放
    ```c
    esp_codec_dev_handle_t spk = bsp_audio_codec_speaker_init();
    esp_codec_dev_set_out_vol(spk, 50);
    esp_codec_dev_sample_info_t fs = { .sample_rate=22050, .channel=1, .bits_per_sample=16 };
    esp_codec_dev_open(spk, &fs);
    // esp_codec_dev_write(spk, pcm, pcm_len);
    esp_codec_dev_close(spk);
    ```
  - 麦克风录音
    ```c
    esp_codec_dev_handle_t mic = bsp_audio_codec_microphone_init();
    esp_codec_dev_sample_info_t fs = { .sample_rate=16000, .channel=1, .bits_per_sample=16 };
    esp_codec_dev_open(mic, &fs);
    // esp_codec_dev_read(mic, buf, len);
    esp_codec_dev_close(mic);
    ```
  - 按键初始化
    ```c
    button_handle_t btns[BSP_BUTTON_NUM] = {0};
    bsp_iot_button_create(btns, NULL, BSP_BUTTON_NUM);
    ```
  - LED 效果
    ```c
    led_indicator_handle_t leds[1] = {0};
    bsp_led_indicator_create(leds, NULL, BSP_LED_NUM);
    led_indicator_start(leds[0], BSP_LED_BREATHE_SLOW);
    ```
  - SD/SPIFFS 挂载
    ```c
    bsp_spiffs_mount();
    bsp_sdcard_mount();
    // FILE *f = fopen(BSP_SD_MOUNT_POINT "/hello.txt", "w");
    ```

## 八、构建与集成
- 头文件入口：`#include "bsp/esp-bsp.h"`（聚合到 `esp32_s3_korvo_1.h`）
- CMake：组件已注册，按 IDF 版本自动选择依赖驱动；需要 `fatfs`、`esp_adc`，私有依赖 `spiffs`、`esp_psram` 等
- 通过 `idf_component.yml` 可由 ESP‑IDF 组件管理器引用；示例路径：`../../examples/audio`

## 九、维护与许可证
- 许可证：Apache 2.0（LICENSE）
- 维护状态：actively developed（README 徽章）
- 版本信息：
  - `idf_component.yml`：version = 3.0.1
  - `CHANGELOG.md`：v1.1.1（2024‑09‑20，修复 IDF 5.0 下 ADC 构建）
  - 注：两处版本标注存在不一致，实际以组件清单发布版本为准

## 十、注意事项与建议
- 音频：
  - `bsp_audio_init(NULL)` 的默认参数为播放 22.05kHz/单声道/16bit、录音 16kHz/TDM；如需更改采样信息，请传入自定义 I2S 配置
  - 该板有 3 麦阵列，采集端使用 I2S TDM；示例默认启用 TDM 部分槽位（源码中为 SLOT0/1），多通道算法需自行处理
- SD 卡：默认 SDMMC 1-bit，若需 SPI 可用 `bsp_sdcard_sdspi_mount()`；FAT 长文件名需在 IDF 配置中启用
- 按键：6 键共享同一 ADC 通道，电压阈值已在 `esp32_s3_korvo_1.c` 中硬编码，若硬件误差较大需自行调整
- I2C：使用 Driver‑NG；IDF <5.3 时建议升级以满足依赖组件（esp_codec_dev）版本要求
- 错误处理：启用 `BSP_ERROR_CHECK` 时错误将触发 assert，发布版本可关闭以返回错误码自行处理

